```
┌───────────────────────────────────────────────────────────┐
│                     Сервер M (Monitoring)                 │
│                 /opt/nve-rsync-host-monitor/              │
│                                                           │
│  ┌─────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   FastAPI   │    │   SQLite DB  │    │   Templates  │  │
│  │  (main.py)  │◄──►│ monitoring.db│    │  (HTML/CSS)  │  │
│  └─────────────┘    └──────────────┘    └──────────────┘  │
│         ▲                                      ▲          │
│         │ POST /api/data                       │ GET /    │
│         │                                      │          │
│  ┌──────┴──────┐                        ┌──────┴──────┐   │
│  │   REST API  │                        │   Web UI    │   │
│  └─────────────┘                        └─────────────┘   │
└───────────────────────────────────────────────────────────┘
          ▲                                      ▲
          │ JSON (curl)                          │ Browser
          │                                      │
     ┌────┴────┐                            ┌────┴────┐
     │         │                            │         │
 ┌───┴───┐ ┌───┴───┐                    ┌───┴───┐ ┌───┴───┐
 │   S   │ │   T   │                    │ User  │ │ User  │
 │ Host  │ │ Host  │                    │   1   │ │   2   │
 └───────┘ └───────┘                    └───────┘ └───────┘
  /opt/nve-rsync-host-source/
  /opt/nve-rsync-host-target/

*(S) Source хост - сервер на котором размещены исходные данные которые бакапируются с помощью rsync
*(T) Target хост - сервер бекапирования на котором размещены копии каталогов и файлов с Source
*(M) Monitor     - сервер который отображает в графической форме результат синхронизации данных с S на T

```
